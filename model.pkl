import os
import streamlit as st

st.write("Current Folder:", os.getcwd())

for root, dirs, files in os.walk("."):
    for file in files:
        if file.endswith(".pkl"):
            st.write("Found:", os.path.join(root, file))
